# --- Веб-фреймворк и сервер ------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20        # разбор form-data для OAuth2-формы входа

# --- База данных -----------------------------------------------------------
sqlalchemy==2.0.36
asyncpg==0.30.0                 # асинхронный драйвер PostgreSQL (рабочий режим)
psycopg[binary]==3.2.3          # синхронный драйвер — нужен Alembic
alembic==1.14.0                 # миграции схемы БД

# --- Валидация и настройки -------------------------------------------------
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0          # требуется типом EmailStr

# --- Безопасность ----------------------------------------------------------
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1                   # версия зафиксирована: с bcrypt>=4.1 passlib
                                # выдаёт предупреждение о чтении __about__

# --- Разработка и тесты (необязательно) ------------------------------------
# pytest==8.3.4
# pytest-asyncio==0.25.0
# httpx==0.28.1
# ruff==0.8.4
